apiVersion: v1
kind: Service
metadata:
  name: redcap-nginx-svc
  labels:
    app: redcap-nginx
spec:
  selector:
    app: redcap-nginx
  ports:
  - protocol: TCP
    port: 1080
    targetPort: 1080
  type: ClusterIP

